import {COLORS} from '../../../common';

export const AppLogo = () => {
  return (
    <h1 style={{marginBottom: '32px'}}>
      tur<span style={{color: COLORS['yellow-dark']}}>.</span>app
    </h1>
  );
};
